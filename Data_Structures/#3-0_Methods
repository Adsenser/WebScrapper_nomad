#days_of_week = ["mon","tue","wed","thu","fri","sat","sun"]

"""
String이나 int등은 그 자체로 많은 기능들을 가지고 있다. 이를 단순히 print만 하면 똑같지만 string.capitaliza() .뒤에 무엇을 넣느냐에 따라 파이썬의 기능들을 사용할 수 있다.

Methods는 데이터에 연결된 함수 즉, 결합된 함수이다. 데이터 안에 있는 것이다.
"""

name = "nico" #이건 단순 변수이다
print(name.startswith("n"))
print(name.replace("o","aa"))
print("nico".replace("i","kk")) #굳이 변수로 안써도 된다
