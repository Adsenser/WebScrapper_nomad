winner = 15

if winner !=10:
  print("if")

#파이썬은 위에서부터 순차적으로 처리하기 때문에 if, elif,else 사용시 위에서 TRUE값이 한번 나오게 되면 그 밑의 조건들은 바로 무시된다. 여기서도 elif가 TRUE이지만 if에서 이미 TRUE가 반환되었으므로 콘솔에는 IF만 출력되었다.
elif winner <= 25:
  print("elif")

else:
  print("else")
