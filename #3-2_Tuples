days = ("mon", "tue","wed")

print(days[0])
print(days[-1])

"""
튜플은 리스트와 거의 동일하다. 하지만 튜플은 그 값들을 수정할 수 있다는 제한을 가진다. 따라서 ~~.함수 를 사용할 때 사용할 수 있는 함수가 극히 제한된다. 

순서는 0부터 시작하게 되는데 만약 뒤에서부터 카운트 하고 싶다면 -1,-2 순으로 하면 된다
"""
